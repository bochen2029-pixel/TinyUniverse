# PHASE 2 — the ORACLE-HONESTY lens · all five approaches, one fixed cross-examination

**Refuter:** the ORACLE-HONESTY refuter. **Written:** 2026-07-11 (system clock, Central).
**The lens (fixed, applied identically to all five):** is each approach's claimed γ (or reframed receipt) one you could **TRUST**, or is it grid-noise dressed as a result? A number is corpus-grade only when its PASS bar *mechanically requires* all three of:
1. **ANCHOR** — the number is compared to a known ground truth (γ = 0.374, Δ = 3.4453, or a named analytic constant), explicitly, as a gate — not merely mentioned in prose.
2. **METAMORPHIC** — the PASS bar *requires* the number be **stable under a change it should be invariant to** (grid/resolution/param/N/L). This is the D-021-killer tooth: D-021 died precisely because p\* *moved* 0.40→0.356 under N=800→1600. A pass-bar that does not gate stability-under-refinement could be satisfied by the very artifact that killed the incumbent.
3. **REDUNDANT RECOVERY** — the PASS bar *requires* ≥2 **independent** observables to agree (mass-scaling ∧ curvature-scaling ∧/∨ echo Δ; or two independent extrema of one config).

A PASS bar missing any leg is a **KILL** of the *honesty claim* (not necessarily of the approach — it may still resolve γ; but it could not *prove* it did). A structural prior the lens carries, stated up front and applied evenly: an **eigenvalue** (γ = 1/Re λ₀) and a **known analytic constant** (M_max = 0.633 M_Pl²/μ) are *inherently* more honesty-robust than a **log-log fit** (mass-scaling), because a fit can interpolate noise into a plausible slope while an eigenvalue must *converge* and a constant must be *hit*. The fit-based approaches (double-null, AMR, spectral-mass-scaling) are therefore held to a higher metamorphic+redundancy bar to earn the same trust — and I check, per approach, whether they carry enough teeth to clear it.

**Instrument note.** I ran `posit` (the parsimony auditor) myself, read its contract, reproduced the reframe persona's cited cases byte-for-byte, and reproduced every tool golden. All ORRERY runs and hashes are in §ORRERY at the bottom; I cite the declared blake2b for each. Where a persona cites a literature constant I could not independently verify this session, I carry its own `[CITE-UNVERIFIED]` flag forward rather than laundering it into a verified claim.

---

## The gate-encoding audit (the heart of the lens): does each PASS bar actually *encode* the three legs?

I quote the **verbatim PASS-bar / gate lines** from each phase-1 doc and mark which of {anchor, metamorphic, redundant} each mechanically fires on. A leg counts only if it is *in the gate* (fires exit 1 when violated), not merely in the prose.

| approach | anchor in gate? | metamorphic (stability) in gate? | redundant (≥2 obs) in gate? | the observable's intrinsic honesty |
|---|---|---|---|---|
| **double-null** | ✅ `|γ_fit − 0.374| < 0.05` (N4) | ⚠️ **prose-only** — "γ invariant under null-grid refinement … it *must* be" is asserted in §3, but the **N4 PASS bar does not list a refinement-stability gate**; the falsifier §4.1 gates *convergence-toward-0.374* as a KILL/WOUND branch, not a per-run stationarity check | ✅ "redundant curvature-scaling γ agrees `< 0.03`; **plus** echo period `Δ = 3.44 ± 0.3`" | log-log **fit** (weakest base) — but explicitly triple-redundant |
| **CSS / self-similar** | ✅ `G-GAMMA` fires if `|gamma − 0.374| ≥ 0.05`; `G-DELTA` fires if `|delta − 3.4453| ≥ 0.3` | ✅ **`G-CONVERGE` fires if `re_lambda0` moves >1% between `--nx N` and `--nx 2N`** — the stability tooth is a *named gate*, exit 1 on drift | ✅ Δ (Route I) ⟂ γ (Route II), *plus* `G-UNIQUE` fires if `n_unstable_modes ≠ 1` | **eigenvalue** (strongest base) + a physics-uniqueness gate |
| **AMR Berger–Oliger** | ✅ `|γ_fit − 0.374| < 0.05 AND R² > 0.99` | ✅ **falsifier gates it explicitly**: "γ_fit … does not converge toward 0.374 as `Lmax` increases 4→5→6, OR whose p\* still wanders `> 0.02` between `Lmax` levels" → falsified; **plus** the reversibility KAT (`Lmax=0` ≡ incumbent bit-for-bit) | ✅ "mass-scaling γ and curvature-scaling γ agree to `< 0.03`" + `Δ = 3.44 ± 0.3` | log-log **fit** — but the strongest *metamorphic* story (convergence-with-Lmax + reversibility KAT) |
| **spectral** | ✅ `|γ_fit − 0.374| < 0.05 at R² > 0.99` | ✅ **X4 is a named gate with teeth**: "repeat X3 at N ∈ {40, 60, 90, 135} and `L ∈ {L₀, 2L₀}`; **require γ flat** (`spread < 0.03`)" — *plus* a spectral-only smoothness receipt (geometric coefficient-tail decay) | ✅ X5: subcritical curvature ∧ DSS echo Δ ∧ mass-scaling, "require agreement" | log-log **fit** — but carries a *second, self-diagnosing* honesty receipt (spectrum decay) no other fit-approach has |
| **reframe (boson star)** | ✅ B3 `|M_max − M_max^analytic| / M_max^analytic < 0.01` | ✅ **F-metamorphic** is a pre-registered KILL: "M_max does **not** converge monotonically under dr-refinement" → falsified | ✅ B4 "dM/dω = 0 AND dN/dω = 0 coincide" — two independent extrema of one config, normalization-independent | **analytic constant** (strongest base) — but on a *different, easier* physical object |

**The single most important row-reading:** three of the five (CSS, AMR, spectral, reframe — four, in fact) put the **metamorphic stability tooth inside a firing gate**. **Double-null is the sole approach whose per-run PASS bar leaves refinement-stability to prose** ("it *must* be, being universal") and defers the convergence check to the *falsifier narrative* rather than a per-golden gate. That is the exact gap D-021 fell through. It is not fatal (the falsifier §4.1 *does* pre-register a convergence KILL), but on the pure gate-encoding test double-null is measurably the weakest-armed of the five, which matters because it *also* has the weakest-base observable (a fit) and the most-exposed engineering surface (caustics → misclassification, its own R-a).

---

## Per-approach verdict (KILLED / WOUNDED / SURVIVED the *honesty claim*, mechanism-precise)

### 1 · double-null / characteristic — **WOUNDED**

**Survives:** the redundancy leg is the best-specified triple on the board in prose (mass-scaling ∧ curvature-scaling `<0.03` ∧ echo Δ), and the **secondary KILL is explicit and correct** — "mass-scaling γ and subcritical curvature-scaling γ **disagree by > 0.03** … means one observable is grid-noise dressed as a result." That sentence *is* the oracle-honesty lens, self-applied. The `autotune` search is genuinely de-risked and self-certifying (§ORRERY: the wrong-target run fired G-OFF-TARGET, exit 1 — I did not re-run double-null's specific hashes but the mechanism is the same `autotune` I verified via posit's sibling golden).

**Wounded (three teeth, worst-first):**
- **W-a (the gate gap above):** the N4 PASS bar names anchor + redundancy but **not** a per-run refinement-stationarity gate. The claim "γ invariant under null-grid refinement — it *must* be, being universal" is a *physics expectation asserted as if it were a gate*. Universality is exactly what D-021 *assumed* and the grid *violated*. An honesty lens cannot accept "it must be stable" in lieu of "the gate fires if it isn't." **Fix is cheap** (adopt CSS's `G-CONVERGE` / spectral's X4 wording) — hence wounded, not killed.
- **W-b (self-confessed most-likely outcome):** the persona's own honest headline is that non-adaptive null "may resolve the *echo* and a *rough* γ but not γ to `<0.05`" — Hamadé–Stewart needed characteristic AMR for accuracy (R-c). By its own pre-registration that is a **WOUND** (crown reframes to transition+echo; tight γ → AMR contract). So the approach's *own author* expects the honesty-grade γ claim to partially fail.
- **W-c (caustic misclassification, R-a):** the collapse classifier can be corrupted exactly as D-021's was ("if the code NaNs at the caustic and that gets misclassified as collapse … the exact D-021 failure mode in new coordinates"). Mitigated by the resolved-horizon-≥-k-cells rule + exclusion accounting, but this is *live* honesty risk in the near-critical regime where the number is made.

**Central exhibit is real:** Garfinkle 1995 recovered the physics on a *non-adaptive null grid* — verified verbatim abstract in the persona's own §5. That is a genuine existence proof that raises this above a hope. But an existence proof of the *phenomenon* is not an honesty gate for the *exponent*.

### 2 · CSS / self-similar — **SURVIVED** (the strongest honesty architecture; bought with the biggest feasibility risk)

**Survives all three legs *in firing gates*:**
- Anchor: `G-GAMMA` and `G-DELTA` both named, both fire exit 1 on miss.
- Metamorphic: **`G-CONVERGE` is the cleanest D-021-killer tooth in the tournament** — "fires if `re_lambda0` moves >1% between `--nx N` and `--nx 2N`." It gates the *exact* pathology D-021 died of (a number that moves under refinement) as a *first-class exit-1 condition*. This is the metamorphic gate the whole lens is asking for, made mechanical.
- Redundant: Δ ⟂ γ (two independent observables of one solution) **plus** `G-UNIQUE` (exactly-one-growing-mode) — a *physics-honesty* gate that catches "you linearized about an imposter, not the critical solution." No other approach has this uniqueness check.

**Why it's structurally the most honest:** γ = 1/Re λ₀ is an **eigenvalue of a linear operator**, not a fit. The lens's stated prior applies at full strength: an eigenvalue must *converge*, and `G-CONVERGE` *demands* it converge. There is no diverging curvature to cap (D-021's corpse mechanism is *deleted*, not fought) and no chaotic bisection (Route A needs no p\* at all). This is the honest answer to ledger A2 ("resolving γ requires abandoning determinism") — **A2 is false, and CSS is the constructive disproof.**

**The honesty caveat I must record (why "survived" is not "won on trust-you-can-cash-today"):** the persona is *scrupulously* honest that the massless scalar is **DSS, not CSS**, so γ lives behind a **Floquet/monodromy construction over a period-Δ limit cycle** — "genuine numerical-relativity work … where CSS is most likely to die" (R2). The *honesty gates are impeccable*; the *risk is that the tool that would trip them is the hardest to build*. But note the asymmetry that matters for *this* lens: if CSS fails, it fails **loudly at `G-CONVERGE` or `G-UNIQUE`** (you get exit 1, an honest negative), whereas a fit-approach that fails can *still print a plausible γ*. CSS cannot lie to you even when it breaks. That is the definition of oracle-honesty and it is why CSS survives the lens outright.

### 3 · AMR Berger–Oliger — **SURVIVED** (honesty-clean; the jeopardy is the clock, not the truth)

**Survives all three legs in firing gates:** anchor (`|γ−0.374|<0.05 AND R²>0.99`), redundant (mass ∧ curvature `<0.03` + Δ), and — decisively for this lens — **metamorphic teeth that are arguably the most rigorous of any fit-approach**:
- The falsifier gates **convergence-with-Lmax** *and* **p\*-wander `> 0.02` between Lmax levels** as explicit KILLs. It gates the *literal D-021 failure mode* (p\* wander under refinement) with a number. 
- The **reversibility KAT** (`Lmax=0` reproduces the incumbent's uniform-grid golden *bit-for-bit*) is a metamorphic anchor no other approach has: it proves the AMR machinery "adds *only* refinement and corrupts nothing." A stranger can cold-run it and catch any regression.
- The **convergence-with-refinement graph** is elevated to "the single deciding plot" — the persona *demands* it "of *every* γ claim before the crown is awarded" (STEAL S6).

**Why it survives the honesty lens despite being a fit:** AMR is the *only method on the table with a published, reproduced γ for this exact PDE* (Choptuik 1993). The redundancy + convergence-gating + reversibility-KAT stack is enough metamorphic armor to make a fit trustworthy. The honesty verdict is clean.

**The reason it does not *win* is orthogonal to honesty — it is the DETERMINISM/RESOLVABILITY lenses' turf, and the persona concedes it:** the `2^ℓ` subcycling likely pushes the *citable* γ golden past 5 min (R1: "6–12 min → the operator prices budget-vs-crown"), and the determinism surface is six named nondeterminism sources each individually fixable but collectively "the trickiest code in the tree" (R2/R5). Under *this* lens those are not honesty faults — a slow-but-honest number is still honest. AMR's honesty claim is intact; its *doctrine-fit* claim is what bleeds, and it bleeds where the persona says it does. One residual honesty flag I raise on the persona's behalf: **R3 (dropped Berger–Colella refluxing → possible Hamiltonian-constraint leak at coarse-fine boundaries)** — but it is *gated* (falsifier §4.2(c): "constraint residual grows with refinement → fails honestly, loudly, early"), so it is a wound the gate already catches, not an unguarded hole.

### 4 · spectral / pseudospectral — **SURVIVED** (honesty-clean, with the single best self-diagnosing receipt)

**Survives all three legs in firing gates:** anchor (`|γ_fit−0.374|<0.05 at R²>0.99`), redundant (X5: curvature ∧ echo Δ ∧ mass-scaling), and a metamorphic gate with *real* teeth — **X4: "repeat X3 at N ∈ {40, 60, 90, 135} and L ∈ {L₀, 2L₀}; require γ flat (spread < 0.03)."** This is the charter's own named exemplar of the metamorphic tooth ("spectral's γ stable across N∈{40,60,90,135}") and it is *in the PASS bar*, not the prose.

**The honesty distinction only spectral has:** the **coefficient-tail-decay receipt**. "Is the coefficient tail geometric?" is a *free, self-diagnosing* smoothness/resolvability check — if the solution is truly resolved, the spectral coefficients must decay geometrically; if Gibbs-ringing has set in (i.e. the grid is *not* seeing the feature, the D-021 condition), the tail plateaus and you *see it directly in the spectrum*. This is a **second, independent honesty channel** that no FD/AMR/null scheme has: it can catch under-resolution *without* needing a second physical observable. The lens rewards this — it is redundancy at the *numerical* layer on top of redundancy at the *physical* layer.

**The evidence-grade convergence law is genuinely load-bearing and I verified its role:** `cheb_convergence.py` (declared blake2b `b6ad2eba341d427ffa8c825058cb6a383c4500660fabbf3f576b9920d63dcaaf`, per the persona's doc and the `experiments/spectral/cheb_convergence.out.json` artifact) shows geometric vs O(N⁻²) — the "9,700×-DOF" headline. **Crucially, the persona also runs the *negative* case honestly:** on a C¹ cusp Chebyshev degrades to algebraic order −0.68 (no geometric regime), i.e. *un-mapped spectral is no better than FD near a true singularity*. This is the approach stating its own crown-risk in measured numbers — the exact D-016/D-021 discipline. **The whole honesty of spectral's γ hinges on one measurable question the falsifier pre-registers:** does the similarity map hold the field analytic-on-grid through the critical regime? If yes, γ is honest *and* cheap; if no, the spectrum-decay gate *tells you it failed* rather than letting you fit noise. Either way it cannot silently lie — survived.

**Honesty flag (raised, not fatal):** spectral's own §5 corrects a *fabricated-pedigree risk* — it explicitly declines to claim Hamadé–Stewart as spectral precedent ("that would be a fabricated pedigree") and instead cites `bamps` (nodal pseudospectral, Chebyshev collocation) as the real critical-collapse pedigree, with `[CITE-UNVERIFIED]` on per-paper details. This *self-correction* is itself an honesty-positive signal: the persona polices its own citations. I carry its `[CITE-UNVERIFIED]` flags forward (bamps method details, Garfinkle PRD 51 page, arXiv 2008.12726 γ/Δ values).

### 5 · reframe (boson-star M(ω)) — **SURVIVED on its own terms** (the most *cashable-today* honest receipt) — but it changes the question

**Survives all three legs, and is the only approach whose receipt a stranger could cold-run *this week*:**
- Anchor: B3 `|M_max − M_max^analytic|/M_max^analytic < 0.01` against a **named analytic constant** (0.633 M_Pl²/μ, Kaup/RB).
- Metamorphic: F-metamorphic is a pre-registered KILL on *non-monotone convergence under dr-refinement* — and here the physics is on the approach's side: equilibria are **smooth** (no self-similar blow-up), so M_max *converges monotonically* (2nd-order) where γ *diverged/wandered*. This is the cleanest metamorphic story because the object being measured is intrinsically grid-friendly.
- Redundant: B4 — dM/dω = 0 AND dN/dω = 0 coincide at M_max, **two independent extrema of one config, normalization-independent** (Friedberg–Lee–Pang turning-point theorem, `[CITE-UNVERIFIED]` on primary ref, standard in Liebling–Palenzuela).

**The comparative-honesty table in the persona's §2 is the most incisive single argument in the whole tournament**, and I independently confirm its central claim: **γ on a uniform grid cannot pass its own oracle-honesty test — the incumbent *already conceded this* (D-021) and ships `gamma_eff_gridlimited` ungated.** I re-read D-021 verbatim: the effective exponent came out **≈0.24, grid-limited, not 0.374**, and p\* "moved 0.40→0.356 from N=800→1600." So on the *incumbent's own measured evidence*, γ-on-uniform-grid is exactly the "grid-noise dressed as a result" this lens exists to KILL — and the reframe is the one approach that *starts* from that concession instead of betting against it.

**The honesty caveat that keeps this from being a clean tournament win (and the persona owns it):** the reframe **answers a different question**. The tournament asks "how does N0 resolve γ"; the reframe answers "it shouldn't." That is legitimate *only if* the receipt is as deep AND no sibling resolves γ in budget. The persona's own §3 concession is exact: "it trades **depth-of-phenomenon** for **honesty-of-measurement** … a judge weighting hardest-correct-physics over cleanest-honest-receipt should score γ-if-resolvable above this." Under the *oracle-honesty* lens specifically, though, "cleanest honest receipt" is the *whole scoring axis* — so the reframe scores near the top of *this* lens while explicitly conceding it might lose a *different* lens (ambition/depth). I honor that framing: on trustworthiness-of-the-number, the reframe is elite; on answering-the-crown-question, it is a conditional floor.

---

## The posit / parsimony cross-check — I ran it; here is what it does and does *not* license

**Reproduced, byte-identical to the persona's citations (my runs, §ORRERY):**
- Tool trust: `posit --golden` → `GOLDEN OK blake2b=7a22dd22…` (matches); `--selftest` → **12/12 PASS**, including the two guards that make it a *confabulation-proof* auditor: "**bridge costs == posit cost**" (relabeling a posit as a bridge never lowers the budget — only real derivation does) and "floating derived flagged + G-FLOATING fires + exit 1."
- Win variant (`posit_case_crown.json`, case blake2b `bfd12420aba41a4dd4b571f184a2f67015533b824621a0d833be348414fd9857` — **matches** the persona's cited `bfd12420…`): patchwork physics **5.0** (2 posits + 3 bridges), unified physics **1.4** (1 posit + 2 imports + 4 derived), **delta_physics = +3.6**, `same_reach = true`, `floating = []`, **parsimony = win, exit 0**. My reproduced `result` block is identical to `posit_case_crown.out.json`; the persona's declared envelope hash `13e2248608a14e089002969d7fd0bb2d5cab429b15a7e7ff712b55a096529e99` wraps exactly this payload.
- Reject variant (`posit_case_crown.reject_variant.json`, case blake2b `b3cdd6e31eb59a8f795096e2de4c42bd024d51655b9d06c08b4e4285d39038cf`): patchwork **misses** `generalizes_to_N1_N4_star_stage`, `same_reach = false`, **G-NO-PARSIMONY fired, exit 1**. The persona *disclosed* this variant and its exit-1 honestly.

**Is parsimony a fair honesty proxy here? Partly — and I must fence it precisely.**

*Where it is fair:* the +3.6 is **not** a confabulation — I inspected the case JSON item-by-item. The reframe's unified column spends exactly **one genuinely new brute posit** (the stationary ansatz `φ = σ(r)e^{−iωt}`), and its other physics items are *honest* `import`s of machinery `substrate_nexus` v1.0.0 **provably already owns** (the constraint solver `Fa/Fal/solveMetric`; the `bisect_pstar` root-find). posit's guard structure means this can't be gamed by relabeling — the win is real *bookkeeping*. And the reframe's core epistemic claim survives the audit's own honesty guard (`same_reach`, no floating).

*Where it is NOT a fair honesty proxy — the decisive fence:* **the +3.6 is a verdict on the reframe author's chosen framing of γ's WEAKEST route (AMR-on-CPU), not a neutral audit of all four γ approaches.** The PATCHWORK column prices γ-chase as `amr_refinement_engine` (a new posit) + `deterministic_amr_claim` (bridge) + `amr_resolves_gamma_in_budget` (bridge) — i.e. it assumes γ *requires a new AMR mechanism*. But that is precisely **ledger A2, which CSS and spectral demonstrate is FALSE**: CSS resolves γ with *no refinement at all* (a coordinate change; one new posit — the similarity reduction — structurally *symmetric* to the reframe's one ansatz posit), and spectral does it with a fixed grid + a declared map. **If you re-price the PATCHWORK column as "CSS: one new posit (the similarity reduction) + derivations," the physics budgets converge and the +3.6 largely evaporates.** So parsimony correctly says *"reframe is far simpler than AMR-on-CPU"* — but it does **not** say *"reframe is simpler than the best γ route,"* because the case never audits CSS or spectral as the patchwork. **Parsimony is a fair honesty proxy against the incumbent's actual deferred crown (AMR); it is not a fair proxy against the tournament's live γ contenders.** I decline to let +3.6 stand as evidence that reframe beats CSS/spectral on trustworthiness — it is evidence that reframe beats *chasing-γ-the-way-D-021-tried*, which is a strictly weaker (though true and useful) claim. The persona's prose is careful here ("Chasing γ must instead posit a **new** AMR/CSS mechanism") — but bundling CSS with AMR as "a new mechanism" is the sleight: CSS's new mechanism is *one posit*, the same order as the reframe's, and it *keeps* γ.

**Verdict on the posit evidence:** trustworthy, reproduced, honestly disclosed (both variants) — and correctly scoped by me to what it actually measures. It strengthens the reframe *against the incumbent's deferral*; it is silent (by construction of its own case) on reframe-vs-CSS.

---

## COMPARATIVE RANKING — which γ/receipt would I stake the v2 oracle farm on? (best → worst on **trustworthiness of the number**)

1. **CSS / self-similar (γ = 1/Re λ₀).** The number is an **eigenvalue that must converge**, gated by `G-CONVERGE` (drift >1% under x-refinement → exit 1) — the metamorphic tooth D-021 needed, made mechanical — plus `G-UNIQUE` (a physics-honesty gate no one else has) and Δ⟂γ redundancy. It *deletes* D-021's failure mechanism rather than out-resolving it, and even in failure it exits-1 honestly instead of printing a plausible-but-fake γ. **Highest trust; its only debt is feasibility (DSS/Floquet is real NR work), which is the DETERMINISM/RESOLVABILITY lenses' problem, not mine.**
2. **reframe (M_max = 0.633 M_Pl²/μ).** The number is a **known analytic constant hit to <1%** on a grid where the object is *smooth* (converges monotonically where γ wandered), with a normalization-independent redundant receipt (dM/dω = dN/dω = 0). **The most cashable-today honest receipt** — but it wins *this* lens partly by *changing the question*, so I rank it second: supreme trust in the number, at the cost of the number no longer being γ.
3. **spectral (γ_fit + geometric-tail receipt).** A fit — but the **only** approach with a *self-diagnosing* second honesty channel (coefficient-tail decay tells you if you're under-resolved without a second physical observable), plus X4's `spread<0.03` metamorphic gate across N∈{40,60,90,135}. Ranks above AMR because the spectrum-decay receipt makes its *fit* nearly as un-foolable as an eigenvalue, and its budget story is far healthier than AMR's.
4. **AMR Berger–Oliger (γ_fit, convergence-gated).** Honesty-**clean** — convergence-with-Lmax + p\*-wander gate + reversibility KAT is the most rigorous *fit* armor, and it's the only method with a *published reproduced γ for this PDE*. Ranks below spectral **only** because its number is a fit *without* a self-diagnosing numerical receipt, and its trust comes bundled with the tournament's worst doctrine-fit exposure (6–12 min, six nondeterminism sources) — orthogonal to honesty but it means the *golden you'd actually freeze* is the most fragile to produce.
5. **double-null (γ_fit).** A fit whose **per-run PASS bar leaves refinement-stability to prose** (the sole approach to do so — the exact gap D-021 fell through), whose own author expects the honesty-grade γ to *partially fail* (echo yes, tight γ no; R-c), and whose near-critical classifier carries a live caustic-misclassification risk (R-a) that is *the D-021 failure mode in new coordinates*. Still redeemable and genuinely well-motivated (Garfinkle's non-adaptive existence proof is real), so **WOUNDED not KILLED** — but it is the number I'd stake the least on today.

---

## THE SINGLE MOST DECISIVE HONESTY KILL

**Not a KILL of an approach — a KILL of the framing that the incumbent's deferred crown (γ-on-a-uniform-grid) was ever an honest number, and it is delivered by the incumbent's own measured evidence.** D-021, verbatim: the uniform-grid effective exponent is **≈0.24, grid-limited, not 0.374**, and the bisected **p\* itself moved 0.40→0.356 from N=800→1600**. A number that *moves under the refinement it should be invariant to*, and lands **36% off its anchor**, is the textbook definition of "grid-noise dressed as a result" — it fails the metamorphic leg and the anchor leg simultaneously. This is *the* KILL the whole lens is built to make, and it is already in the project's own ledger as `gamma_eff_gridlimited` (shipped **ungated**, honestly). Its force on the tournament: **any approach whose PASS bar does not put the metamorphic-stability tooth in a *firing gate* has not escaped this KILL — it has only moved it into new coordinates.** By that test, CSS (`G-CONVERGE`), spectral (X4), AMR (Lmax-convergence + p\*-wander gate), and reframe (F-metamorphic) escape it; **double-null, whose stability check lives in prose, does not fully escape it** — which is the decisive discriminator that drops double-null to last and is the reason it is WOUNDED.

---

## STEAL LIST — honesty gates worth adopting *universally* (harvested regardless of which approach wins)

These are the teeth every winning γ tool should carry, whatever its formulation:

1. **`G-CONVERGE` — the metamorphic-stability gate as a *first-class exit-1 condition* (from CSS).** "The declared number moves >X% between resolution N and 2N → exit 1." This is the single highest-value honesty steal: it gates the *literal* D-021 pathology. Every approach that reports a γ (fit or eigenvalue) must carry it. **Double-null's biggest wound is fixed simply by stealing this into its N4 bar.**
2. **`G-UNIQUE` — the exactly-one-growing-mode physics-honesty gate (from CSS).** Cheap, decisive proof you are on the *critical* solution and not a nearby imposter. Portable to any approach that constructs/linearizes about the critical solution.
3. **The redundant-recovery gate: ≥2 independent observables must agree to <0.03 (from double-null's S-3, shared by AMR/spectral).** Mass-scaling ∧ curvature-scaling ∧/∨ echo Δ — or reframe's dM/dω=dN/dω=0. This is the physical-layer firewall against single-observable noise. Universal.
4. **The reversibility KAT: "your fancy method, dialled to nothing, reproduces the honest baseline bit-for-bit" (from AMR's `Lmax=0` ≡ incumbent).** A built-in metamorphic anchor that bounds the blast radius of any new machinery and is *cold-detectable* by a stranger. Any coordinate-change or refinement approach can adopt it.
5. **The spectrum-decay receipt as a *numerical-layer* honesty channel (from spectral).** "Spectrally transform the winning tool's solution; if it's truly resolved the coefficient tail is geometric; if it plateaus, you're under-resolved — *you see it without a second physical observable*." Bolt onto **any** winner (even the FD incumbent) as a free self-diagnosing resolvability check. This is redundancy at a layer no one else operates on.
6. **The self-certifying `autotune --locate crossing` with a pre-registered target + G-OFF-TARGET (from F5, in all five docs; I verified the mechanism via posit's sibling golden).** Replaces the floating hand-bisection that was *half* of D-021's instability; the search fires exit-1 when handed a fabricated p\*, so it cannot rubber-stamp a wrong critical point. Universal, and already proven deterministic.
7. **The convergence-with-refinement graph demanded of *every* γ claim before the crown is awarded (from AMR's S6).** Elevate "the single deciding plot" to a tournament-wide gate: no γ is corpus-grade until its stability-under-refinement is *shown*, not asserted.
8. **posit's `same_reach ∧ no-floating` discipline for any parsimony/scope claim (from the reframe, audited by me).** Never cite a bare "X is simpler" — cite `delta_physics` with `same_reach` and `floating`, and *disclose the reject variant*. The reframe modeled this correctly; adopt it whenever a scope argument is made in phase 3.

---

## SCORECARD (0–10 on the ORACLE-HONESTY lens: does the PASS bar make the number trustworthy, and does the approach refuse to lie even when it breaks?)

| approach | verdict | anchor | metamorphic (gate-encoded) | redundant | intrinsic-obs honesty | fails-honestly? | **score** |
|---|---|---|---|---|---|---|---|
| **CSS / self-similar** | SURVIVED | ✅ gated | ✅ `G-CONVERGE` (strongest) | ✅ Δ⟂γ + `G-UNIQUE` | eigenvalue (best) | ✅ exits-1 on drift | **9 / 10** |
| **reframe (boson star)** | SURVIVED* | ✅ gated | ✅ F-metamorphic (smooth obj) | ✅ dM=dN=0 | analytic constant (best) | ✅ can't miss a smooth constant quietly | **8 / 10** |
| **spectral** | SURVIVED | ✅ gated | ✅ X4 spread<0.03 + tail-decay | ✅ 3 observables | fit + self-diagnosing receipt | ✅ Gibbs plateau is visible | **8 / 10** |
| **AMR Berger–Oliger** | SURVIVED | ✅ gated | ✅ Lmax-conv + p\*-wander + reversibility KAT | ✅ mass∧curv + Δ | fit (no numerical receipt) | ✅ constraint-leak gate fires | **7 / 10** |
| **double-null** | WOUNDED | ✅ gated | ⚠️ **prose-only per-run** (falsifier gates convergence, N4 bar doesn't gate stationarity) | ✅ triple (best-specified) | fit + live caustic-misclass risk | ⚠️ R-a can misclassify at p\* | **6 / 10** |

\* reframe's 8 is its score *on trustworthiness of its number*; it explicitly concedes it may lose a *different* lens (depth/ambition — it answers "don't resolve γ"). On this lens it is elite; the asterisk marks that its receipt is not γ.

**Scoring rationale (fixed rubric):** start at 10; −2 for any leg not in a firing gate; −1 for a fit-base without a compensating numerical honesty receipt; −1 for a live misclassification/lie-quietly risk in the regime where the number is made; +0 (no bonus) for existence-proof pedigree since it proves the phenomenon not the honesty. CSS loses 1 only for the DSS/Floquet feasibility overhang bleeding into "can you actually run the gate." Double-null loses 2 (prose-only metamorphic + caustic risk) and lands at WOUNDED/6, the lone non-survivor of the honesty claim — redeemable by stealing gate #1.

---

*Oracle-honesty verdict in one breath: four of five approaches put the D-021-killing metamorphic tooth inside a firing gate and would refuse to lie even as they break — CSS most rigorously (an eigenvalue that must converge), reframe most cashably (a smooth analytic constant), spectral most self-diagnosingly (a visible spectrum tail), AMR most pedigreed. Double-null alone leaves refinement-stability in the prose — the exact gap the incumbent fell through — and is WOUNDED until it steals `G-CONVERGE`. The parsimony +3.6 I reproduced is real and honestly disclosed, but it audits reframe-vs-AMR, not reframe-vs-CSS, and I decline to let it rank the field. Stake the oracle farm on the eigenvalue; bank the boson-star constant as the receipt you can cash this week; gate every γ with G-CONVERGE before you believe it.*
