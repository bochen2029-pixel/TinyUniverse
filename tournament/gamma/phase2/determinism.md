# PHASE 2 — REFUTATION UNDER THE **DETERMINISM** LENS (the γ fork)

**Refuter:** the DETERMINISM refuter. **Lens (CHARTER §"three refuter lenses"):** every mechanism a γ claim leans on must be buildable as a **byte-deterministic, golden-freezable, single-file CPU tool inside the ~5-min budget** — `(params,seed)→byte-identical declared output`; a frozen `(params)→blake2b` golden; exit `0`/`1`/`2` never conflated. A step that needs a **nondeterministic solver**, an **unbounded / over-budget runtime**, a **floating tie-break** in the declared path, or a **research-grade library** that won't fit the single-file doctrine is a **KILL of the claim that leans on it** — not necessarily of the physics, but of the physics *as an N0 golden*.

**The concrete referent for "deterministic single-file golden."** `C:\TinyUniverse\substrate\substrate_nexus.cpp` v1.0.0 (contract FROZEN, golden `13aa73e5`): one `.cpp`, C++17, fp64, stdlib-only, no GPU; RK4-in-time with the metric re-solved each substage by **outward Heun/trapezoidal integration of the log-metric ODEs** (guaranteed-positive `a,α`, no linear solve, no iteration, inherently sequential); a `blake2b-256` over a **declared JSON whose hash domain excludes `notes`** (D-013); `exit 0` all gates pass · `1` a declared gate fired · `2` error; **~20 s at the frozen `N=800, r_max=24`.** That is the box. Every approach below is scored on whether its *added* mechanism keeps every one of those properties.

**Standing epistemic honesty.** The five EKG "gears" (double-null, CSS, AMR, spectral) and the reframe's `bosonstar_nexus` **do not exist yet** — so every γ-resolution verdict here is `[ARGUMENT-GRADE]`, reasoned from the contract each persona specified plus the incumbent's measured determinism idiom. What **is** evidence-grade is the **ORRERY-arming**: I reproduced every persona's cited `autotune`/`posit` receipt against the tools in `C:\ORRERY` (read-only), byte-for-byte. Those receipts certify the **search primitive** each approach rides (the `--locate crossing` p\*/eigenvalue finder), **not** γ. I keep that line sharp throughout.

---

## 0 · ORRERY-arming — receipts reproduced (evidence-grade, read-only)

**Instrument trust (run first, CWD `C:\ORRERY`):** `python tools\autotune\autotune.py --selftest` → **SELFTEST PASS** (6/6, including the negative control "wrong pre-registered target → G-OFF-TARGET fires → exit 1"). `--golden` → **`GOLDEN OK blake2b=c79002f23cf236baab5ecdb5753603a7a3853199f750b0139771d4e6cdd55bbe`** (matches every persona's cited anchor). `posit --golden` → **`7a22dd229a42ce46a6c102f0545f83022b975dc39d5f1794cd6019e6f5a20e44`** (matches reframe's cited baseline). The autotune contract confirms the determinism basis I am leaning on: **no RNG, no wall-clock, `--seed` is inert**, canonical `%.6f` serialization, hash domain `{seed,params,result,gates,verdict}`.

**Every cited receipt reproduced byte-for-byte.** I recomputed each hash with the tool's own serializer (`declared_object` for the autotune-declared convention; raw-stdout for the reframe convention — see the note), and where a persona shipped a driver I ran *their* driver, not an inferred command.

| persona | receipt (tag) | my reproduction | cited blake2b | verdict |
|---|---|---|---|---|
| **all** | autotune self-golden | `GOLDEN OK` | `c79002f2…` | ✅ **MATCH** |
| **all (shared)** | charter-arming crossing @0.5, exit 0 | xloc=0.500000, exit 0 | `db490a3111b770996fcec05b9bc59981f35395980be5f1ad617101dd13552c80` | ✅ **MATCH** |
| **double-null** | DN-locate-pstar @0.334, exit 0 *(ran their `run_autotune.py`)* | xloc=0.334000, on_target, exit 0 | `26ec96dea28874a60730c15628eea966d610667e5a56beae5074617cd8c25ac5` | ✅ **MATCH** |
| **double-null** | DN-locate-wrongtarget @0.360 *(neg control)* | on_target=**false**, **G-OFF-TARGET fired**, **exit 1** | `371b72f65d9a25b200886890d5f9bdc131f53302bddcccf5307394c79fd52838` | ✅ **MATCH** |
| **AMR** | positive locate @0.5, exit 0 | xloc=0.5, exit 0 | `db490a31…` | ✅ **MATCH** |
| **AMR** | neg-control @0.60, exit 1 | on_target=false, **exit 1** | `bf7023ee399c91946e010eab1fb0bd39c7aa686e40c05a38013dd31e37329c10` | ✅ **MATCH** |
| **AMR** | tight-tol (tol 0.005), exit 0 | xloc=0.5, exit 0 | `6c6e2adac5c7fd549e1eda4d0ba73c2466165a21061240f0760b1be9307a5131` | ✅ **MATCH** |
| **CSS** | Run A crossing @0.37, exit 0 | xloc=0.369950, exit 0 | `6dd1fb038bd12a69795732d7a142e317c454dad9196c93c1424e475d73344585` | ✅ **MATCH** |
| **CSS** | Run B crossing @0.374, exit 0 | xloc=0.374004, exit 0 | `0602eb2d6a02b03060826087c12239077afed315ce2f609a277ff5511f857acd` | ✅ **MATCH** |
| **spectral** | crossing @0.5, exit 0 | xloc=0.5, exit 0 | `db490a31…` | ✅ **MATCH** |
| **spectral** | argmax @0.374, exit 0 | xloc=0.374014, exit 0 | `27c8cb4719f8a6e2dd455e386cd3da63ab1a9630896ab11a084a29c004d04eeb` | ✅ **MATCH** |
| **spectral** | `cheb_convergence.py` (self-declared hash, ran the script) | self-declared hash reproduced, exit 0 | `b6ad2eba341d427ffa8c825058cb6a383c4500660fabbf3f576b9920d63dcaaf` | ✅ **MATCH** |
| **reframe** | posit E1 (n0_crown_choice, ran the case) | patchwork phys=5.0, unified phys=1.4, Δ=+3.6, same_reach=true, parsimony=win, exit 0; `case_blake2b=bfd12420…` | `13e2248608a14e089002969d7fd0bb2d5cab429b15a7e7ff712b55a096529e99` | ✅ **MATCH** |
| **reframe** | autotune E2 groundstate @0.8267, exit 0 | xloc=0.826702, err 2e-6, exit 0 | `11b87fb84cb8f7d9bb884073a293febf00f835c1b09ac766f7e7c92f7b1ea9ea` | ✅ **MATCH** |

**No falsification.** Every cited hash reproduces. **One methodological finding worth recording (not a fault in the number):** the **hash-domain convention is not uniform across personas.**
- double-null, AMR, CSS, and spectral cite the **`declared_object`** hash (ORRERY D-013 domain: envelope *minus* `tool`/`version`/`notes`, `%.6f`) — the correct, tightest determinism domain.
- **reframe** (E1 posit, E2 autotune) cites the **blake2b of the raw CLI stdout *including* `tool`/`version`/`notes` and the trailing newline** (verified: `11b87fb8…` = blake2b of the raw `.out.json` bytes; `13e22486…` = blake2b of the posit raw stdout + `\n`). The *values reproduce deterministically*, so this is not a fabrication — but it is a **weaker declared domain**: it folds non-declared fields (`notes` is explicitly non-declared in both tools) and a trailing byte into the hash. Under the incumbent's own rule ("notes excluded"), reframe's cited hash convention would need to be tightened to `declared_object` before its golden freezes. Flagged for phase 3; it does not change reframe's determinism verdict (the reframe *tool* it argues for is trivially deterministic — see §5). *(Two earlier reconstructions of mine mismatched purely because I inferred the command/params instead of running the persona's stored driver; the fair reproduction — their driver, their convention — matches. The lesson is itself a determinism datum: reproduce via the frozen command, not a paraphrase.)*

**What the receipts do and do not establish (uniform across all five).** They establish — evidence-grade — that the **F5 search fix** (a pre-registered `--locate crossing` against a fixed target, replacing the incumbent's floating 20-iter hand-bisection that let p\* wander 0.40→0.356) is deterministic, byte-reproducible, and **self-certifying**: it fires a gate and exits 1 on a wrong target, so it cannot rubber-stamp a fabricated p\*. They establish **nothing about γ** — every objective is an analytic sigmoid/logistic stand-in, and every persona labels it so. The EKG solver that would make γ evidence-grade is, in all five cases, unbuilt.

---

## 1 · DOUBLE-NULL / characteristic

### Claim-by-claim

- **SURVIVED — the field update is byte-deterministic and single-file.** The null-diamond rule `(rφ)_{i+1,j+1} = (rφ)_{i+1,j} + (rφ)_{i,j+1} − (rφ)_{i,j} − ∬ src` (and the single-null RK-in-`u` with outward radial constraint quadrature) is a *fixed arithmetic sequence* — structurally the incumbent's idiom (explicit fp64, single-thread, no RNG, no atomics, no reduction). Golden-freezable exactly as `substrate_nexus` is. This is the strongest determinism structure of the four EKG approaches: **no adaptivity at all in the base scheme**, the grid is static in `(u,v)`. The persona's own charter-arming + p\*-locate receipts (`db490a31…`, `26ec96de…`, neg-control `371b72f6…` exit 1) reproduce byte-for-byte — the *search* half is evidence-grade-deterministic **now**.

- **WOUNDED — the caustic-exclusion collapse criterion has a data-dependent branch (R-a).** Near-critical, ingoing characteristics can cross; the persona's *own* mitigation is "terminate the slice at a **resolved** apparent horizon (`2m/r→1`, horizon ≥ k cells wide) *before* the caustic; runs that caustic first are **excluded and counted**." Determinism-wise this is a **classifier with a floating boundary**: "did a resolved horizon form before a caustic?" is a comparison of two fp64 events whose ordering can flip under a 1-ULP perturbation for a pulse tuned to `p*`. It does **not** break `(params,seed)→byte-identical` for a *fixed* `p` (the arithmetic is deterministic, so the exclusion decision is deterministic for that run). But it means the **γ-fit sample set is a step function of `p`**, and the fit's declared output can change discontinuously across a hair-thin `p`-band — the exact shape that let the incumbent's p\* wander. **Patch cost, not mechanism change:** the fix is the persona's already-named "resolved-horizon width gate + exclusion accounting," which is deterministic *per run*; the residual risk is that the *fit* is sensitive to which runs got excluded, which is an oracle-honesty problem (redundancy gate) more than a determinism kill. **Verdict: wounded, patchable with cost, no new nondeterminism in the declared path.**

- **WOUNDED (budget) — R-c: tight γ may need characteristic AMR.** The persona concedes Hamadé–Stewart (1996) needed characteristic AMR for *accuracy*, and pre-registers that non-adaptive null resolving only the echo + a rough γ is a **WOUND** (crown → transition+echo; tight γ → an AMR contract). Under my lens this is the honest most-likely partial outcome: the *non-adaptive* claim is byte-clean and in-budget; the *tight-γ* claim inherits AMR's determinism-surface problem (§3) the moment it needs refinement. The base cost is fine (O(N²) diamond, N~few×10³, ~50 evolutions → minutes). **Verdict: the base tool survives; the tight-γ crown is budget/AMR-wounded, as the persona pre-registered.**

- **SURVIVED — exit codes & golden plumbing.** Identical to `substrate_nexus`; trivial.

**Net:** double-null's determinism story is **strong** — a static null grid is the second-most-deterministic mechanism on the board. Its only determinism wound is the near-critical caustic/horizon classifier (a floating-boundary *classifier*, deterministic per run, patchable), and its crown risk is a *budget/AMR* wound it declared itself, not a determinism kill.

---

## 2 · CSS / SELF-SIMILAR (the eigenvalue crown)

### Claim-by-claim

- **SURVIVED (and best-in-class) — γ = 1/Re λ₀ is a linear-algebra number, the ideal golden shape.** Extracting γ from a *single eigenvalue of a linear operator on a coarse fixed grid* is the most golden-friendly path in the tournament: no time-evolution, no diverging curvature to cap, no chaotic bisection. A fixed-shift **inverse-iteration for the single dominant mode** (the persona's declared choice, *not* a general QR/Arnoldi) is deterministic and byte-reproducible. The declared vector `{Δ, Re λ₀, γ, R², n_unstable}` → blake2b is small and stable. **This is the strongest determinism-of-the-crown-observable claim anywhere in the field.**

- **KILLED (as an N0 golden, this cycle) — the DSS Floquet *construction* is not a single-file, in-budget, tie-break-free build; it is genuine NR research (R2/R3).** This is the load-bearing kill and the persona flags it as their own biggest risk. The elegant "ODE eigenvalue problem" is **literally true only for CSS (radiation fluid)**; the **massless scalar is DSS**, so the crown requires: (i) solve a **τ-periodic PDE two-point BVP** demanding periodicity `Z(τ+Δ,·)=Z(τ,·)` **plus regularity at the center *and* at the coordinate-singular past self-similarity horizon** (a series/L'Hôpital treatment of a singular point), with **Δ itself an eigenvalue** found by a shooting/relaxation iteration; then (ii) a **monodromy/Floquet eigen-extraction** around the period. Under the DETERMINISM lens the failing steps are precise:
  1. **The BVP shooting/relaxation is an iterative root-find whose iteration count and convergence path are data-dependent** — the persona's §3 concedes "the schedule risk is the `construct` BVP's iteration count and horizon-regularity handling, which could blow the 5-min budget or need care I can't guarantee blind." An iterative solver *can* be made deterministic (fixed iteration cap, fixed initial guess, fixed tolerance, no wall-clock stop) — but a **fixed-cap BVP that has not converged emits a wrong γ**, so determinism and correctness collide: you either freeze a possibly-unconverged number (honest-oracle kill) or let it run to a data-dependent stopping point (determinism kill). Threading that needle for a singular-point DSS BVP is exactly the "care I can't guarantee blind."
  2. **Regularity at the past self-similarity horizon is a singular-point boundary condition** — the numerically delicate part; a stiff shoot here is where iteration counts explode. This is not "add cost," it is a *mechanism* whose deterministic-and-in-budget realization is unproven and is real NR-paper-scale work (Gundlach spent a paper on it).
  A patch here **changes the mechanism** (you would fall back to route B — dynamical targeting — which reintroduces a p\*-tune and a full EKG evolution, i.e. you are no longer doing the clean eigenvalue crown, you are doing double-null/AMR-style collapse with a similarity coordinate).

- **WOUNDED — route B (dynamical targeting) is deterministic but is no longer "the eigenvalue crown."** If `construct` is infeasible in-budget, the persona's fallback is to *evolve* the full EKG in `(τ,x)` and read Δ off the in-place echo. That evolution is deterministic (RK4-in-τ, same idiom) — **but it needs T\*** (a fit, mitigated as a periodicity-sharpness `autotune` argmax — deterministic) and it is a *collapse* run, inheriting the same near-critical sensitivity as the incumbent, just in nicer coordinates. It delivers Δ (route I), not the clean γ=1/Re λ₀. **Verdict: the determinism-cheap thing CSS can actually build this cycle is Δ, not the eigenvalue-γ.**

- **SURVIVED — the CSS *fluid* warm-up (Evans–Coleman β≈0.36) is a clean, deterministic ODE eigenvalue solve.** This is a genuine, in-budget, single-file, golden-freezable receipt — a *fixed-point* (not limit-cycle) ODE eigenvalue problem. It ships as a lesser crown regardless. It just is **not the scalar γ**.

**Net:** CSS owns the **best determinism-of-the-observable idea in the tournament** (γ as one eigenvalue) — and that idea is the tournament's top STEAL. But the *scalar* crown it must deliver lives behind a **DSS Floquet BVP whose deterministic, in-budget, single-file construction is unproven NR research**, which the persona candidly names as where CSS most likely dies. Under this lens the eigenvalue-γ crown is **killed as a buildable N0 golden this cycle**; the fluid-β prototype and the Δ route survive.

---

## 3 · AMR (Berger–Oliger)

### Claim-by-claim

- **SURVIVED (impressively engineered) — the 1-D restricted BO scheme eliminates every *classical* AMR nondeterminism source by construction.** The persona did the hard work and I credit it: **fixed regrid schedule** (regrid every `K=8` base steps — a pure function of the step counter, *never* a convergence/wall-clock trigger); **strict `>` flagging vs a fixed constant `χ_thr`** with `==`→not-flagged (no percentile, no top-k, **no ties to break**); **1-D interval-merge clustering** (integer arithmetic on a sorted index list — the 2-D Berger–Rigoutsos ambiguity collapses); **pre-order, patch-index-ordered, single-thread tree walk** (no parallel reduction, no atomics); **one ordered radial pass** for the composite metric (the same Heun integrator, sequential by construction). Each is individually correct. The reversibility lemma (`Lmax=0` ≡ incumbent bit-for-bit) is a genuinely good built-in metamorphic anchor. This is the **most rigorous determinism self-audit** of any approach — and its `db490a31…`/`bf7023ee…`/`6c6e2ada…` search receipts reproduce byte-for-byte.

- **KILLED (as an N0 golden) — subcycling's `2^ℓ` cost busts the ≤5-min budget for the *citable* γ, by the persona's own cost model.** This is the decisive determinism-lens kill for AMR-at-N0, and it is **self-disclosed**: §3.2 prices the full γ golden at **"plausibly 8–20 min"**, with the *likely* case (subcritical curvature-scaling, which needs the deepest levels near p\*) at **"6–12 min → the operator prices budget-vs-crown."** The lens is explicit that **an over-budget runtime is a kill of the claim that leans on it.** The γ crown leans on the deep-`Lmax` scaling ladder; that ladder is over-budget; therefore the **γ-as-N0-golden claim is killed** — cleanly, on the clock, exactly as R1 predicts. Note the shape: this is *not* a nondeterminism kill (the scheme is byte-clean); it is a **budget kill**, which is a distinct blade of the same lens. The persona's mitigation ("ship a `Lmax=4` ~2-min *argument-grade* golden, run `Lmax=6` as a longer cold-run") **concedes the citable γ does not fit the box** — it splits the golden from the citable number, which is honest but is precisely the reframe's and CHARTER's point that N0 is the wrong stage.

- **WOUNDED — the determinism surface is a maintenance hazard (R2), and the missing-refluxing constraint leak (R3) is a physics-honesty risk that also perturbs the declared hash.** Six distinct nondeterminism sources are *each* fixable but *each* is a place a careless refactor silently breaks byte-identity — the persona rates this "higher maintenance burden than any other approach." Under the lens the mitigation (a determinism KAT: fixed snapshot → asserted patch layout → asserted hash, cold-runnable) is **exactly right and should be mandatory** — but its necessity is itself the wound: the approach is one refactor away from a broken golden in a way the incumbent's straight-line integrator is not. Separately, dropping Berger–Colella refluxing (justified for the *constrained* metric) can leak a Hamiltonian-constraint violation at coarse-fine boundaries that grows with refinement — the persona gates it (§4.2c), and honestly, a leak that grows with `Lmax` would show as γ *not converging*, i.e. it fails loudly. **Verdict: wounded, not killed, on the determinism-surface axis — but only because the budget already killed the crown.**

- **SURVIVED — the base machinery is single-file-feasible (~500 lines on the incumbent) with no research library** (no PARAMESH/Carpet/AMReX). Credit given; it is a *restricted, hand-rolled, doctrine-shaped* BO scheme, not a library import. This is why AMR is not killed on the *single-file* axis — only on the *budget* axis.

**Net:** AMR is the **only approach with a published, reproduced γ for this exact PDE** (Choptuik used it), and its 1-D determinism engineering is the most careful in the field — so its *nondeterminism* claims largely **survive**. But the DETERMINISM lens is not only about nondeterminism; it is also about the **≤5-min golden budget**, and AMR's own cost model puts the citable γ golden at **6–20 min**. That is a **KILL of γ-as-N0-golden on the clock**, self-disclosed, with the honest home being N3-GPU where `2^ℓ` subcycling is affordable.

---

## 4 · SPECTRAL / PSEUDOSPECTRAL

### Claim-by-claim

- **SURVIVED — layer-1 (static similarity map) is byte-deterministic and in-budget with margin.** RK4-in-τ + a **dense fixed differentiation matrix `D`** + a **diagonal exponential filter** are all fixed deterministic linear algebra, single-thread → bit-reproducible, the incumbent's idiom exactly. N~10²–10³, O(N²)/step → seconds–minutes. The declared spectral-coefficient vector → blake2b is clean. The persona's convergence-law artifact (`b6ad2eba…`, Chebyshev geometric collapse vs FD2 O(N⁻²), the 9,700×-DOF headline) **reproduces byte-for-byte** via its self-declared hash — an evidence-grade *method* claim (not a γ claim). **This is a legitimately strong determinism-and-budget story for the base scheme.**

- **WOUNDED — layer-2 (feedback-rescaled map) is determinism-CONDITIONAL, by the persona's own audit.** `T*` is not known a priori; if it drifts, the fix is a map rescale. The persona pre-registers that this is deterministic **iff** the control law is *one closed-form scalar update from a declared observable* (e.g. `L_{n+1}=L_n·g(ξ_peak^n)`), and that **a data-dependent multi-domain *re-split* would not be** — "if γ needs true multi-domain adaptivity whose *topology* responds to data, the DETERMINISM lens wounds the claim and I fall back to layer-1 + fixed domain decomposition." I take the pre-registration at face value: **the single-scalar feedback is deterministic** (a pinned formula on a declared scalar — genuinely unlike a Berger–Oliger tree whose *topology* is data-dependent); the **topology-adaptive re-split would be a KILL**, and the persona has already agreed to fall back rather than ship it. **Verdict: wounded but self-fenced — the deterministic fallback (layer-1 + *fixed* domain decomposition) is a real, buildable position.**

- **WOUNDED (conditioning, not nondeterminism) — `‖D²‖~O(N⁴)` stiffness can balloon the step count.** If the formulation exposes the second-derivative operator, `dτ≲C·N⁻⁴` and the step count (hence runtime) explodes — a *budget* risk. The mitigation (keep the system **first-order** so the binding operator is `D`, `dτ~N⁻²`; or implicit-split the stiff linear part with a **fixed dense LU** — deterministic, bit-reproducible single-threaded) is sound and keeps determinism. **Verdict: wounded on budget-conditioning, deterministically patchable.**

- **KILLED (contingent, and this is the honest crown risk) — if the map cannot hold the field smooth-on-grid through the critical regime, the spectrum Gibbs-rings and the un-mapped spectral EKG fails near p\* exactly like FD.** The persona's *own* `cheb_convergence.py` measures Chebyshev degrading to algebraic order **−0.68 on a C¹ cusp** — i.e. un-mapped spectral is no better than FD at a true singularity. This is primarily an **oracle-honesty** kill (grid-noise-dressed-as-γ), but it has a determinism face: the fallback to rescue smoothness is layer-2 adaptivity, and *aggressive* layer-2 (topology re-split) is the determinism kill above. So the two risks are coupled: **the harder the map must work to hold smoothness, the closer layer-2 slides toward the nondeterministic re-split.** The persona pre-registers this exact coupling and would rather be killed honestly than fake γ. **Verdict: the crown is contingent on a measurable smoothness question; if it fails, spectral is killed — but the determinism of the *base tool* is not the thing that kills it.**

**Net:** spectral's *base* determinism (layer-1) is **strong and in-budget** — comparable to double-null, and it ships a unique **self-diagnosing determinism/resolvability receipt** (spectrum-decay). Its determinism *wound* is layer-2: a single-scalar feedback map is deterministic and the persona fences off the topology-adaptive re-split that would be a kill. Its real exposure is oracle-honesty (does the map hold smoothness), not determinism.

---

## 5 · REFRAME THE CROWN (boson-star M(ω))

### Claim-by-claim

- **SURVIVED — comfortably, and by the widest margin. A static ODE shoot has no time-evolution, no chaotic near-critical regime, no floating collapse criterion — it deletes all three sources of the incumbent's p\* wander.** `(σ_c, ω-bracket, grid)→byte-identical (ω,M,R₉₉)`. It is the incumbent's *own* constraint integrator (the `Fa`/`Fal`/`solveMetric` machinery, verbatim) with a **static** scalar source `4πr(ω²/α²±μ²)a²σ²+σ'²` — outward RK4, `ω` bisected on the large-r divergence sign, ADM mass from the mass aspect. The ground-state find **is** `autotune --locate crossing` mechanically (E2 receipt `11b87fb8…` reproduces, xloc=0.826702, err 2e-6, exit 0). **This is arguably a *stronger* determinism story than the incumbent's evolution battery** — the persona's claim, and I concur under the lens: no evolution means no CFL, no dissipation-tuning, no near-critical stiffness. Runtime: ~600–1200 outward integrations of a 3-ODE system on ~10³ points → **seconds**, orders of magnitude inside budget.

- **SURVIVED — golden-freezable with the full D-016 triad present.** Anchor = `M_max≈0.633 M_Pl²/μ`; metamorphic = **monotone convergence under dr-refinement** (the *opposite* of γ's chaotic wander — refining makes it *better*); redundant = the convention-independent **coincident `dM/dω=0` ∧ `dN/dω=0`** turning point. All three legs buildable and deterministic. The reframe's Occam receipt (posit E1, `13e22486…`, Δphysics=+3.6, same_reach=true, parsimony=win, exit 0) reproduces byte-for-byte — evidence-grade that at equal reach the reframe spends **one** new posit (the stationary ansatz) vs chasing-γ's new-mechanism-plus-three-unbacked-bridges.

- **WOUNDED (bookkeeping, not physics) — the reframe's cited hash convention is the raw-stdout-including-`notes`-and-newline domain, which is looser than the incumbent's "notes excluded" (D-013) rule.** As recorded in §0: `11b87fb8…` and `13e22486…` are blake2b over the *full CLI stdout* (with the non-declared `notes` field and a trailing `\n`), not over the `declared_object` the other four personas hash. The numbers reproduce deterministically, so this is not a fabrication and not a determinism *failure* — but before `bosonstar_nexus`'s golden freezes, its declared hash **must** be over the declared object (notes excluded), matching the incumbent. **This is a trivial fix (the incumbent already implements exactly the right domain) and does not touch the reframe's dominant determinism win.** Flagged for phase-3 hygiene.

- **SURVIVED — exit codes clean; the deciding experiment is the *cheapest* in the tournament** (reuses the incumbent almost entirely, adds no evolution). The only non-determinism-lens exposure is **depth-sufficiency** (is a bound-state mass a worthy N0 crown vs a universal exponent) — an *ambition* question the persona owns, out of scope for this lens. On the determinism axis there is nothing to kill.

**Net:** the reframe **passes the DETERMINISM lens more cleanly than any other approach** — it is a static shoot on the grid the incumbent already runs, deterministic by construction, in-budget by orders of magnitude, D-016-triad-complete. Its only determinism-adjacent blemish is a **looser cited hash-domain convention** (fixable, trivial). Its real contest is fought on the *other two* lenses (does it duck the crown), not this one.

---

## COMPARATIVE RANKING — determinism-feasibility (best → worst)

1. **REFRAME (boson-star M(ω)) — 9/10.** A static ODE shoot on the incumbent's own grid: no evolution, no near-critical chaos, no floating classifier; deterministic by construction, seconds not minutes, D-016 triad complete. The *only* determinism-lens blemish is a looser cited hash convention (raw-stdout incl. notes), trivially fixed. **Passes the box with the widest margin.**
2. **DOUBLE-NULL — 7/10.** A *static* null grid (no base-scheme adaptivity) with a byte-deterministic diamond/RK-in-u update, single-file, in-budget. Wounded by the near-critical caustic/horizon *classifier* (floating boundary, deterministic per run, patchable) and by the pre-registered tight-γ→characteristic-AMR budget wound. The most-deterministic mechanism that also has a credible path to γ.
3. **SPECTRAL — 6/10.** Layer-1 (static map) is byte-clean, in-budget, and ships a unique self-diagnosing determinism receipt (spectrum-decay). Docked because the crown may force layer-2, whose *topology-adaptive* variant is an outright determinism kill (the persona fences it off to a deterministic fallback), and because `D²` stiffness is a real budget-conditioning risk.
4. **AMR — 5/10.** The *nondeterminism* is engineered away with unusual rigor (fixed schedule, integer 1-D clustering, no ties, no reduction, reversibility lemma) — that part genuinely survives. But the lens also polices the **≤5-min golden budget**, and the persona's own cost model puts the citable γ golden at **6–20 min**: a clean **budget KILL of γ-as-N0-golden**, plus the highest determinism-maintenance surface of any approach. Superb engineering, wrong stage (its home is N3-GPU).
5. **CSS — 4/10.** Owns the tournament's best determinism-*of-the-observable* idea (γ = one eigenvalue, inverse-iteration, tiny stable declared vector) — but the *scalar* crown lives behind a **DSS Floquet BVP with singular-point regularity whose deterministic, in-budget, single-file construction is unproven NR research** (the persona's own biggest risk). The clean eigenvalue story is *literally* the CSS-fluid case; the massless scalar is one dimension harder and its build is where CSS most likely dies. Ranked last on *feasibility-as-an-N0-golden-this-cycle*, despite having the highest ceiling if the construction ever lands.

*(Note the ranking is on determinism-feasibility only. It deliberately inverts "physics depth": AMR/CSS chase the deepest, hardest-to-build receipt; reframe/double-null chase the most-buildable. The other two lenses re-weight this.)*

---

## THE SINGLE MOST DECISIVE KILL

**AMR's citable γ golden busts the ≤5-min budget — 6–20 min by the persona's own cost model (§3.2).** This is the most decisive KILL because (a) AMR is the *reference* method — the one approach with a published, reproduced γ for this exact PDE, so its failure is the most consequential; (b) the kill is **self-disclosed and quantitative** (`Lmax=6` subcycling with the subcritical-curvature ladder → "6–12 min," full γ golden → "8–20 min"), not a hunch; (c) it is a **clean budget blade of the determinism lens**, not a messy nondeterminism argument — AMR's nondeterminism is actually *cured*, which makes the budget kill all the sharper: the method *works* and is *deterministic* and still **cannot be an N0 golden because it will not finish in the box.** The mitigation (ship a shallow `Lmax=4` argument-grade golden, run the citable number as a longer cold-run) *concedes the crown does not fit N0* — which is exactly the reframe's thesis and the proposal's own scoping (γ → N3 `horizon`, where `2^ℓ` subcycling is affordable). Runner-up kill: **CSS's DSS-Floquet construction is unbuilt NR research, not a single-file weekend tool** — a kill of feasibility rather than of the clock.

---

## STEAL LIST — deterministic tricks worth taking regardless of who wins

1. **`autotune --locate crossing` against a pre-registered target, replacing hand-bisection (F5).** *Evidence-grade-deterministic today* (every persona's neg-control fires G-OFF-TARGET → exit 1; verified byte-for-byte). This cures the **search half** of D-021's wander — the incumbent's floating 20-iter bisection is *a* cause of p\* moving 0.40→0.356 — independent of which grid wins. **Highest-value, already-proven steal.** Bolt it into whatever tool ships.
2. **AMR's "no Berger–Colella refluxing because the polar-areal metric is constraint-solved, not fluxed."** The constrained (not free) metric has *no conservative flux across coarse-fine boundaries*, so the single hardest, most-nondeterminism-prone part of general AMR simply **does not exist** here. Any refinement/coordinate approach that keeps constrained evolution inherits this. (Corollary: injection restriction suffices for the fields; the metric is re-solved by the ordered radial pass.)
3. **CSS's γ = 1/Re λ₀ via fixed-shift *inverse-iteration* for the single dominant mode.** The most golden-friendly γ-extraction on the board: one eigenvalue of a linear operator, converging (not drifting) under refinement, a tiny stable declared vector. **Whoever first *finds* the critical solution (double-null, AMR) should then *linearize about it* and read γ this way** rather than fitting grid-quantized masses near a wandering p\*. Deterministic by construction (fixed shift, no QR/Arnoldi order-sensitivity).
4. **The reversibility lemma as a built-in metamorphic anchor (AMR): "your fancy method, dialled to nothing, must reproduce the honest baseline bit-for-bit"** (`Lmax=0` ≡ incumbent). A cheap, cold-detectable determinism *and* correctness guard any coordinate-change/refinement tool should carry — it bounds the blast radius of a subtle bug to "anything that breaks the baseline hash."
5. **The determinism KAT (AMR): a fixed field snapshot → asserted patch layout → asserted hash, runnable cold in `--selftest`.** For *any* approach whose determinism is non-obvious (AMR, spectral layer-2), this makes a silent byte-break loud and stranger-detectable — it is what turns "I assert determinism" into "a stranger verifies it in one pass."
6. **Spectral's spectrum-decay as a free honesty/resolvability receipt.** "Is the coefficient tail geometric?" is a *self-diagnosing* smoothness check: spectrally transform the winner's near-critical solution — if it is truly resolved, the tail decays geometrically; a Gibbs plateau means grid-noise. Portable to double-null/AMR as a metamorphic gate, and it is a *determinism-adjacent* receipt (a converged deterministic run should show the decay).
7. **Similarity coordinates `(τ,x)` as a resolution primitive that keeps determinism trivial** (CSS/spectral): evolve where the DSS solution is log-periodic so the echo *recurs at fixed x* instead of shrinking off the grid — a static grid, zero adaptivity, determinism for free. Even if neither CSS nor spectral wins the crown, this coordinate trick belongs to whichever formulation ships, and it is the cleanest answer to ledger A2 ("resolving γ need not abandon determinism").
8. **Reframe's "kill the evolution entirely" move — harmonic time dependence makes the source static.** The deepest determinism trick of all: a bound-state receipt has *no time integration*, so *no* CFL/dissipation/near-critical-stiffness surface exists to be nondeterministic. Even outside the crown question, `bosonstar_nexus` is the N4 cross-check the "hydrogen-ball → star" sentence relaxes toward — build it regardless.

---

## SCORECARD — DETERMINISM axis (0–10)

| approach | score | one-line justification (determinism-feasibility) |
|---|---:|---|
| **REFRAME (boson-star M(ω))** | **9** | Static ODE shoot on the incumbent's own grid; no evolution/chaos/floating-classifier; seconds, D-016-complete. Only blemish: looser cited hash-domain (trivially fixed). |
| **DOUBLE-NULL** | **7** | Static null grid, byte-deterministic diamond/RK-in-u, single-file, in-budget. Wounded by near-critical caustic/horizon classifier (floating boundary, patchable) + pre-registered tight-γ→AMR budget wound. |
| **SPECTRAL** | **6** | Layer-1 static map is byte-clean + in-budget + ships a self-diagnosing spectrum-decay receipt. Docked: crown may force layer-2 (topology-adaptive re-split = kill, fenced to a deterministic fallback); `D²` stiffness budget risk. |
| **AMR** | **5** | Nondeterminism rigorously engineered away (fixed schedule, integer 1-D clustering, no ties, reversibility lemma) — but the citable γ golden is **6–20 min (self-disclosed)** → budget KILL as an N0 golden; highest maintenance surface. |
| **CSS** | **4** | Best determinism-*of-observable* idea (γ = one eigenvalue) but the scalar crown needs a **DSS-Floquet singular-point BVP that is unbuilt NR research**, not a single-file in-budget tool — killed as a buildable N0 golden this cycle; fluid-β + Δ survive. |

---

### Coda — what the DETERMINISM lens actually decides

This lens does not pick the deepest physics; it picks what can be **frozen as a byte-identical golden a stranger runs cold in ≤5 min.** On that axis the ordering is stark and a little uncomfortable: the two approaches that chase the *hardest, most-cited* receipt (AMR's reference γ, CSS's eigenvalue-γ) are the two the lens **kills as N0 goldens** — AMR on the clock (self-disclosed 6–20 min), CSS on unbuilt-NR-research feasibility — while the approach that **declines the crown** (reframe) passes most cleanly precisely because a static bound-state shoot has no nondeterminism surface at all. Double-null and spectral sit between: static base grids that are byte-clean and in-budget, wounded only where the *near-critical* regime forces a classifier (double-null) or a smoothness-rescue map (spectral) toward data-dependence. The honest determinism verdict is therefore the reframe's and the proposal's own: **γ as a resolved N0 CPU golden strains or breaks the box for the two approaches that could actually resolve it; its natural home is N3-GPU.** What N0 can freeze *now*, deterministically, is either a bound-state receipt (reframe, cleanest) or a static-null / static-similarity *transition + echo-Δ* receipt (double-null / spectral) — with the F5 `autotune` crossing, the constrained-metric no-refluxing simplification, and the eigenvalue-γ extraction banked as steals for whichever substrate ships. Every verdict here stays `[ARGUMENT-GRADE]` until the pre-registered gears are built and their goldens frozen; the ORRERY receipts that *are* built reproduce byte-for-byte, and the register holds the rest of the doubt.
