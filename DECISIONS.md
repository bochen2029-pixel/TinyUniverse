# DECISIONS.md — architecture decision records

Format: D-NNN · date · status · decision / why / consequences. Reversals get new entries, never edits.

---

**D-024 · 2026-07-12 · ACCEPTED — N3 `curve` findings: geometry curves; the 1919 factor-of-2 decomposed into the N2-time half and the N3-space half; the honestly-weak-field precession gate.**
N3 `curve` gives the substrate a **spatial** metric: geodesics through the weak-field metric ds²=−(1+2Φ/c²)c²dt²+(1−2Φ/c²)dl² reproduce the exact-GR curvature observables. CPU fp64 geodesic oracle (no GPU, like N0). Findings, all measured (D-016/D-021):
**(1) The signature result — light deflection 4GM/bc², decomposed.** A photon ray-traced through the effective index n = √(g_ij/−g_tt) = 1+2GM/rc² deflects by **4GM/(bc²)** (Eddington 1919) — matched to 0.3–0.75% at b=100–200. Running the SAME ray with only the lapse curved (n_lapse = 1+GM/rc², the g_tt = N2 contribution) gives exactly **half** (2GM/bc²), so **full/lapse = 2.004–2.008** (GR: exactly 2). The spatial curvature is literally the other half of the bending — the two rungs (N2 time, N3 space) each contribute one half of the 1919 result. The cleanest possible demonstration that *space* curves.
**(2) Perihelion precession, honestly weak-field.** The relativistic orbit equation d²u/dφ²+u = GM/L² + 3GM u²/c² integrated perihelion→perihelion gives Δϖ per orbit. First tried at a=100 (p=75): measured **5.5% ABOVE** the leading formula 6πGM/(c²a(1−e²)) — NOT a bug: at r_s/r_peri=0.02 the 2PN+ and eccentricity corrections are ~5%, and the isotropic-metric higher-order differs from Schwarzschild's. Per D-018 (measure the regime, don't fake the gate), moved to the genuinely weak orbit a=1000 (p=750, r_s/p=0.0013) where the leading 1PN precession IS the answer: measured **0.52%** (0.72°/orbit). The gate tests "orbits precess at the GR rate" in the regime where that rate is unambiguous — honest scoping, not a widened tolerance hiding higher-order.
**(3) The honest boundary — N3 is the geodesic/curvature ORACLE, not a dynamical GPU metric field.** It proves the static weak-field metric (sourced by the substrate's Poisson Φ; g_tt lattice-validated by N2 `redshiftPM`, 3.6%) bends light and precesses orbits at exact GR. A live GPU metric field a²(x) with back-reaction (the energy density sourcing the metric each step, the full Hamiltonian/momentum constraint) is the harder continuation toward N4/horizon — named, not faked. Weak-field/1PN only; strong-field is N4+. v1 got these observables by *pseudo-forces* (`photons` 0.83%, `keprel` 0.50%); N3 gets them from the *metric* — the emergence.
*Consequence:* **v2 N3 CLOSED** — 2 GREEN goldens (`curve_deflect` `4e6c33ca`, `curve_precess` `67272705`), both two-passed. Tool `substrate/curve_nexus.cpp` (CPU fp64). Contract `curve.contract.md` v1.0.0. Ladder: N0 (oracle) → N1 (field/SP weld) → N2 (the clock/time) → **N3 (geometry/space)** → N4 `star` next. The substrate now carries time AND space curvature — the full weak-field metric, emergent from its own gravity, bending light and precessing orbits at the rates GR predicts.

**D-023 · 2026-07-12 · ACCEPTED — N2 `lapse` findings: the substrate grows a clock; exact Schwarzschild time dilation from the Newtonian potential; the temporal-metric-only honest boundary.**
N2 `lapse` (ROADMAP §3, the recommended immediate build after N1) turns the substrate's Newtonian potential Φ into a **clock**: a per-cell lapse α(x) = √(1 + 2Φ/c²) and a declared, hashed proper-time field τ(x) = ∫α dt. c goes **LIVE** (it was inert in N1's non-relativistic SP). Findings, all measured (D-016/D-021 discipline):
**(1) The redshift oracle is EXACT, not merely weak-field.** For a point mass Φ = −GM/r, the lapse α = √(1 − 2GM/rc²) = √(1 − r_s/r) is *algebraically identical* to the exact Schwarzschild static-observer lapse (g_tt = −(1−r_s/r)c²). So `redshift` (analytic Φ) reproduces gravitational time dilation exactly across weak→strong field: a clock at r ≈ 2 r_s ticks **40% slow** (z = 0.398 = 1/√(1−r_s/r)−1); max |α_meas/α_ana − 1| = **5.7e-6** (gate 1e-3). The proper-time integrator τ = ∫α dt matches α·N·dt to 6.6e-6 (fp32 accumulation floor; gate 5e-5). The machinery oracle, like N1 freepacket/sho3d vs N5.
**(2) The weld — the substrate's own gravity gives correct gravitational redshift.** `redshiftPM` feeds a resolved mass through the N1/M2 PM cuFFT-Poisson (`kGreen` verbatim) → Φ → lapse, then fits the lapse-inverted well Φ_meas = ½c²(α²−1) = −A/r + C: fitted depth **A/GM = 0.9643 (rel 3.57e-2)** — exactly the PM discretization floor N1's `--poissontest` measured (3.6%), gated at 5%. The substrate's *self-consistent* gravity produces gravitational time dilation of Newtonian depth: the emergence claim, bridging M2 (Newtonian Φ) → M4 (relativistic time) *on the lattice*.
**(3) The honest boundary — temporal metric only.** The lapse gives exact g_tt (redshift, time dilation) but N2 does **not** model the spatial curvature (1−2Φ/c²)dx²: **no light bending, no orbit precession, no Shapiro delay** — those are N3 `curve`. Static spacetime, no back-reaction; Schwarzschild-scalar (no frame-dragging). A photon's frequency shift is right; its path is not yet bent. Printed in the contract; the point-mass exactness is the g_tt = weak-field coincidence, honestly scoped (not a claim of full strong-field GR).
*Consequence:* **v2 N2 CLOSED** — 2 GREEN goldens (`lapse_redshift` `e2c75be5`, `lapse_redshiftPM` `3dddb950`), both two-passed (GOLDEN OK on independent re-run). Tool `substrate/lapse_nexus.cu` (single-file CUDA; reuses N1 `kGreen` verbatim — one solver, a third caller). Contract `lapse.contract.md` v1.0.0. Ladder: N0 (oracle) → N1 (SP weld) → **N2 (the clock)** → N3 `curve` next (the metric back-reacts; light bends; the fluid-CSS Stage-A background plugs in as the static oracle).

**D-022 · 2026-07-12 · ACCEPTED — N1 `field` weld findings: the Poisson-coupling 1/N³ bug, the SP soliton demonstrated by self-similar covariance, and the honest fp32 echo.**
The N1 `field` gate is the **PM+ψ gravity weld** — does quantum pressure (the ħ split-step) actually balance self-gravity (the PM cuFFT-Poisson)? Three findings, all measured, none faked (D-016/D-021 discipline):
**(1) The bug that blocked the weld — a spurious 1/N³ in the potential coupling.** The first soliton attempt failed (CONTINUITY: "r_c·M not constant; self-gravity ~3% too weak"). A new energy/virial diagnostic (K spectral, W=½∫mρΦ, virial 2K/|W|, ρ_peak checkpoints) revealed the truth: **W ≈ 1e-8 — SEVEN orders of magnitude too weak**, not 3%. Root cause: the imaginary-time potential kick divided Φ by an extra 1/N³ (`evcoef = m·τ/ħ/N³`). But M2's `kGreen` factor already folds the C2R inverse-FFT normalization (its `/PMN³`) into itself, so the buffer after `cufftExecC2R` holds the **true Φ** (the M2 convention — `dReal now holds Phi`, app line 2199), not Φ·N³. Removed the extra 1/N³ (relaxSoliton, spStepReal, hostSPEnergy). Pinned by a new `--poissontest`: a compact Gaussian source's numerical Φ matches analytic −GM/r to **3.6%** (fitted A=0.1929 vs G·M=0.20). *The lesson mirrors D-018's observable-vs-evolution split: the ψ engine (freepacket/sho3d, green vs N5) was always right; the bug was purely in the Φ normalization, which no gravity-free scenario could expose — only the soliton's virial probe could.*
**(2) The soliton weld — demonstrated by SP self-similar scale-covariance to 3e-8.** After the fix, a genuine self-bound SP soliton forms at M≳200 (total energy E<0). The scale-covariance law r_c·M=const is tested along the **SP self-similar family** (box, dx, seed width, τ all ∝ 1/M for a 2:1 mass pair — so the two discrete lattice problems are literally identical up to the λ=2 rescaling x→x/λ, M→λM): **M=200 (box 16) r_c=1.8008, M=400 (box 8) r_c=0.9004 → r_c·M = 360.1561 for both, scale_rel = 2.97e-8.** Virial 2K/|W|=1.9176 identical for both masses (a fixed periodic-box/mean-subtracted discretization signature; the continuum SP value 1.0 needs box→∞, but its *mass-independence* is exactly what makes the covariance hold). κ=360.156 measured in-tool (no external number gated; literature SP scaling is the labelled cross-check). This is the smallest experiment that can *only* pass if the fusion is correct — M6 never had gravity, M2 never had ħ. Golden `field_soliton` frozen (256³, 2×10000 imaginary-time iters, state `dd4d08d7…`).
**(3) echoF — the "byte-identical" echo is retired as fp32-impossible; the honest receipt is reversibility-to-round-off + byte-exact reproducibility.** The contract claimed a byte-identical Loschmidt echo (fwd N→conj→fwd N→conj == init). **In fp32 this is physically precluded:** the ≈4N cuFFT round-trips are not bit-reversible (a rounded forward-then-inverse FFT ≠ identity). Measured: the round-trip L2 residual grows linearly with N (3.7e-5 @ N=50 → 4.0e-4 @ N=600) — the *physics* reversal is exact; only bit-equality fails. So echoF gates the **residual at round-off** (L2_rel < 1e-3) and freezes the **byte-exact reproducibility** golden (two runs → identical declared bytes, verified — the doctrine determinism). Contract Determinism § corrected (BUILD FINDING); "byte-identical" → "reversible to fp32 round-off". A truly bit-exact echo needs integer/reversible arithmetic — out of scope for cuFFT fp32.
**(4) cloudF — the classical-limit cross-check, built in place of galaxyF.** galaxyF (v1 galaxy rotation curve by the field) is Q-N1-1, the acknowledged hardest gate: a single-valued ψ has curl-free velocity and cannot carry a rotating disk's angular momentum except via quantized vortices (research-grade), and it needs a ~512-su box. Rather than force a likely-unpassable galaxyF (and risk a faked-looking in-band number — the D-016 anti-pattern), the achievable **irrotational** v1-farm cross-check was built: **cloudF** reproduces the v1 `cloud`/`collapse` physics by the field. An overdense uniform sphere (S=0, curl-free → exact ψ map) infalls under self-gravity in real time and **collapses at the analytic free-fall time**: ρ_peak ×756, core 12.0→0.83 su, max compression at **t/t_ff = 0.97** (t_ff=√(3π/32Gρ₀), textbook), then a quantum-pressure bounce (Madelung Q). This is the classical (Q→0) half of the two-sided Madelung claim — soliton is the quantum-pressure ground state, cloudF is the classical infall dynamics.
**(5) mergerF — the two-body cross-check (added same session).** Two self-gravitating ψ-lumps (M=2000 total, sep 12, at rest) **attract under mutual self-gravity**: separation shrinks 11.2→6.0 su (×0.54) on the two-body free-fall time (t/t2b=0.67), forming a denser remnant (ρ_peak ×224). The lump centroids move *only* because gravity pulls them → unambiguous gravitational attraction *between distinct masses* (the PM Poisson makes real gravity between separated sources, not just within one lump). Gate: sep ≤0.6× initial AND remnant ρ_peak ≥2×. Honestly gated on *attraction*, not full virialization (each lump's internal-collapse timescale competes with the orbital time; a clean full merge needs stable-soliton lumps + orbit tuning — future work). *Consequence:* N1 has **6 GREEN field goldens** (freepacket, sho3d, soliton, echoF, cloudF, mergerF); the weld is demonstrated across the full dynamical range — bound quantum ground state + classical collapse + two-body attraction; soliton two-passed + full-suite-confirmed cold (`GOLDEN OK d163d765`). **galaxyF (v1 galaxy rotation curve) is the one deferred scenario** (Q-N1-1: ψ can't carry disk rotation without quantized vortices + a ~512-su box; named future work, not faked). The frame-contract v2.0.0 MAJOR bump stays named-not-executed (N1 is headless-first).

**D-021 · 2026-07-11 · ACCEPTED — N0 crown, honestly: a uniform grid demonstrates the Choptuik *transition* but cannot resolve the exponent; precise γ deferred to AMR.**
The proposal named the Choptuik critical exponent γ ≈ 0.374 as N0's crown golden. **Building it and measuring produced the honest verdict that it is not resolvable on a uniform grid at feasible CPU cost.** Critical collapse is self-similar (discretely self-similar echoing): as the pulse amplitude p → p*, the central curvature grows without bound over ever-finer scales — Choptuik (1993) needed adaptive mesh refinement to see it. A fixed grid *caps* the resolvable curvature (measured directly: the Garfinkle–Duncan subcritical scaling variable saturates at ≈(φ/dr)², the grid ceiling), and refining the grid does not converge — it makes the near-critical regime *chaotic* (the bisected p* itself moved 0.40→0.356 from N=800→1600). What a uniform grid *can* prove, cleanly and reproducibly, is the **Type-II transition**: a sharp critical amplitude p* below which the field disperses and above which a black hole forms, with the horizon mass falling continuously toward zero as p → p* (M_BH,min/M_BH,max ≈ 0.29 over the sampled band; arbitrarily small black holes — the actual Choptuik *phenomenon*). The *far-field* mass scaling there is a clean power law (R² ≈ 0.998) but its effective exponent (≈ 0.24) is grid-limited, **not** 0.374. **Per RAYFORMER (ADR-007) and the D-016 precedent (the 7π withdrawal), N0 ships what it honestly proves and declares what it cannot**: S4 gates the transition (bracketed threshold + mass → 0 + monotone rise) and reports the effective exponent as a *labelled diagnostic* (`gamma_eff_gridlimited`), never gated to 0.374. The precise universal γ is deferred to the AMR contract (N-later; the GPU `horizon` stage may also reach it). Faking a tight γ gate against grid noise would poison every downstream v2 module gated on this oracle — the exact confident-wrong the doctrine exists to prevent. The proposal (§6) anticipated this verbatim: "fixed-grid horizon resolution bounds the smallest honest BH; declare it; AMR is a later contract." *Consequence:* substrate_nexus S1–S5 green (flat-space conservation, subcritical dispersal, supercritical horizon, Type-II transition, massive-KG stability), golden `13aa73e5` frozen at N=800/r_max=24 (~20 s); the crown remains real (critical collapse demonstrated) and honest (its exponent is future work, named).

**D-020 · 2026-07-11 · ACCEPTED — v2 SUBSTRATE track approved; N0 built in-repo as the v2 oracle spine; geometric G=c=1 units.**
Operator directive "go substrate" approves the v2 track (`docs/PROPOSAL_2026-07-12_v2_substrate.md`), whose first act (proposal §7) is `contracts/substrate_nexus.contract.md` + the N0 CPU tool. **Built in-repo** (`substrate/substrate_nexus.cpp`, `goldens/substrate_nexus/`) rather than as a sibling repo `TINY UNIVERSE II`: it mirrors how M0 `tiny_nexus` began, keeps v1 as the adjacent oracle farm (the proposal's whole premise), and the repo-split stays a later, reversible call (N0 lifts cleanly if v2 outgrows this tree). **Units are geometric G = c = 1**, NOT the v1 dials (c=20, ħ=0.5, G=2×10⁻³): the Choptuik exponent and the Type-II transition are dimensionless/universal, so N0 works where the numerical-relativity literature values apply directly. The v1 dials are what make the eventual *GPU* substrate (N1–N4) computable in-box — the proposal's thesis — not what the oracle needs; N0 owns the equations, not dials. blake2b + the declared-JSON/golden idiom are lifted verbatim from tiny_nexus. *Consequence:* substrate_nexus is the standing fp64 oracle for the v2 GPU ladder; ARCHITECTURE §11 gains a v2 row; the harness runs it as a CPU golden regardless of card contention (the foundation stone that needs no GPU).

**D-019 · 2026-07-11 · ACCEPTED — M7 findings: the wrap made live, two golden supersessions with distinct causes, the echo as an exactness receipt.**
The 3-torus stopped being a force-field fact (PM has been periodic since M2) and became a coordinate fact: `kDriftK` canonicalizes live positions into `[−256, 256)` every tick, and the direct/BH kernels take minimum-image separations. **(1) The wrap is bit-reversible by construction:** L = 512 = 2⁹, so the `±512.0f` shift and `roundf(d/512)` are exact fp32 ops, and for `|d| < 256` `mimg(d) = d` byte-for-byte — every pre-M7 pair closer than a quarter-box, and every non-crossing trajectory, is unchanged. **(2) Two supersession causes, kept separate (honesty):** *(a) seam-crossing ejecta* — cloud, galaxy, merger, collapse each fling particles past ±256 over their runs; those coordinates now wrap instead of flying to infinity, so the declared-state hash changes. Physics gates re-verified pass (de/dp/entropy/formation all green); re-frozen under the "go M7" directive (D-016 precedent). *(b) the photon proper-time leak* — `kDriftK`'s τ line evaluated `√(1 + (2Φ − v²)/c²)` for photons too, and in fp32 `v²·(1/c²) = 400·0.0025` rounds to `0.99999997`, leaving `√(3×10⁻⁸) ≈ 1.7×10⁻⁴` of spurious ageing per tick. The `circumnav` gate (photon_tau_zero) caught it. Fix: gate the τ accumulation on `m.w > 0` — **photons are now structurally ageless**, an M4-contract intent finally made exact (light does not age; the clamp only ever hid the leak). This changed only the `photons` hash (that scenario never crosses a seam), re-frozen. **(3) The echo is the receipt:** `merger` (one-way, 12000 ticks) superseded, but `echo` (6000 forward + 6000 reversed, same init) stayed **byte-identical** — a particle that crosses a seam forward (`x − 512`) and back (`+ 512`) returns to the same fp32 bits, so the Loschmidt reversal survives the wrap exactly. That GREEN is the proof the topology change didn't cost determinism. *Consequence:* 5 goldens re-frozen (cloud `9431fff5` · galaxy `2642d510` · merger `1e80bee9` · collapse `579f6986` · photons `3738112d`), 13 byte-identical, 3 new (circumnav `3f18f02c` · expand `ce448f2b` · bubbles `78b753f1`); 21/21. Light-history, little-planet, and bubbleVis are non-declared (never allocated in the golden path), so they touch no hash.

**D-001 · 2026-07-11 · ACCEPTED — Sibling of ORRERY, not part of it.**
ORRERY Invariant 3 mandates headless tools; TU opens a window and runs on an interactive clock. TU adopts the doctrine (contract/golden/oracle/two-pass) as a sibling repo. ORRERY is referenced, never edited, from here. *Consequence:* TU carries its own harness, goldens, and contracts.

**D-002 · 2026-07-11 · ACCEPTED — Path A rendering stack: CUDA + Vulkan external-memory interop + hand-rolled CINEMATIC stack; renderer swappable at the frame contract.**
Custom beauty is a solved problem on this machine (GARGANTUA). UE 5.8 = TextureShare showcase shell only, someday (display-only; Beta since 4.27; UE6 migration clock). Falcor = reference quarry, not foundation (research-framework churn vs single-file doctrine). *Consequence:* CINEMATIC.md is law; frame contract is the swap seam.

**D-003 · 2026-07-11 · ACCEPTED — One binary, two faces (Buddhabrot v4 pattern).**
`tinyuniverse.exe` windowed by default; `--scenario/--steps/--seed/--json` (+`--selftest/--golden`) = headless instrument face with the universal envelope. *Consequence:* no separate tool exe; goldens run the same binary CI runs.

**D-004 · 2026-07-11 · ACCEPTED — Decoherence = Ratchet inscription, not generic CSL.**
Collapse when a bubble's state is redundantly recorded into environment DOF; unwrite bound `min(1, p/((1−p)ρ))^R`, crossover `(1−p)ρ = p` — the closed form ORRERY's `ratchet` tool froze (golden 91fce3c4; MC↔analytic 0.06%). Physically principled (quantum-Darwinism-shaped), measurable in-sim (R is visible!), parameter-anchored. *Consequence:* nexus N6 verifies the math; the sim mapping (what counts as a record) is M3's contract. No metaphysical commitment implied (Q-003).

**D-005 · 2026-07-11 · ACCEPTED — liborrery lifted verbatim.**
`rng.cuh` (counter-based stateless RNG), `reduce.cuh` (deterministic reductions), envelope (blake2b goldens), `ckpt.h` (save format), `regime.h` (derived-only bitmask) copied byte-identical from `C:\ORRERY\lib` at a pinned commit, KAT selftest included. *Consequence:* any divergence is a named fork here; TU inherits the host≠device libm pins.

**D-006 · 2026-07-11 · ACCEPTED — Determinism via fixed timestep + recorded input traces.**
dt = 1/240 s is a dial; sim ticks decouple from render frames; user interventions are recorded per-tick and replayable bit-exact. Golden = (dials, seed, trace, steps) → declared-state hash. Visuals/timing non-declared. *Consequence:* replays, goldens, and netplay-grade reproducibility come from one mechanism.

**D-007 · 2026-07-11 · ACCEPTED — OptiX via function-table dispatch, compile-out fallback (Buddhabrot recipe).**
No .lib link; `nvoptix.dll` from driver; SDK auto-detected, never required. *Consequence:* the M5 RT-vs-compute geodesic measurement (ORRERY's parked `lens` spike, ADR-007 protocol) can ship either verdict without a hard dependency.

**D-008 · 2026-07-11 · ACCEPTED (adoption deferred to M4+) — Slang for shared math.**
Metric/potential/blackbody/redshift math written once, emitted as CUDA (kernels) and SPIR-V (shaders). Khronos-hosted; Falcor-proven. *Consequence:* until adoption, shared math lives in a header included by both sides; the header is written Slang-portable (no CUDA-isms in shared functions).

**D-009 · 2026-07-11 · ACCEPTED — CPU nexus before any CUDA (ASTRA-7 pattern).**
`tiny_nexus.cpp`: fp64, single file, ordered PASS/FAIL battery proving the four regimes compose and freezing dial defaults. It then serves as the standing oracle for every CUDA module. *Consequence:* M0 blocks everything; that is the point.

**D-010 · 2026-07-11 · ACCEPTED — fp32 core, df64 ladder only on measured need.**
Box is 512 su; fp32 holds ~7 digits — fine at world scale. If zoom-as-mechanic exceeds it, adopt Buddhabrot's df64 ladder (and only then consider AstraCoord sectoring). *Consequence:* no premature 128-bit coordinates; the gate is a measured artifact, not a hunch.

**D-012 · 2026-07-11 · ACCEPTED — presentation layer: thin GL blit now (P0), Vulkan external-memory presentation when the SDK lands (P1).**
Measured fact: no Vulkan SDK on this machine (`C:\VulkanSDK` absent, `VULKAN_SDK` unset). All *rendering* is CUDA kernels ending in a finished 8-bit image either way; only presentation touches a graphics API. P0 = GARGANTUA's proven `cudaGraphicsGLRegisterImage` blit (~80 lines, zero new dependencies); P1 = Vulkan swapchain + external memory + timeline semaphores, required for DLSS/Streamline, adopted when the SDK is installed (backlog, with the DLSS milestone). This is a presentation deferral, not a renderer decision reversal — D-002 stands; Q-005's Vulkan-vs-DX12 door stays closed on Vulkan. Also: GARGANTUA's build used `-use_fast_math`; TU builds without it (Invariant 6), accepted perf cost ~nil (workload is memory/atomic-bound). *Consequence:* frame.contract.md v1.0.0 carries the P0/P1 clause; M1's gate is judged on the CUDA pipeline, which survives the P1 swap untouched.

**D-018 · 2026-07-12 · ACCEPTED — M6 findings: observable-vs-evolution coefficients, oracle-isolates-implementation, near-field fringes.**
(1) The shoq energy meter used ħk²/2m·... wrong power of ħ (0.25k² instead of ħ²k²/2m = 0.125k²) — the σ_ground = 0.500 exactness pinpointed it as an *observable* bug, not an evolution bug (measured T was exactly 2× truth). (2) The tunneling oracle initially used a finer grid (dx 0.0625 vs 0.25) — 6% T disagreement from barrier-edge discretization, which tunneling feels exponentially. Corrected principle: **the in-scenario fp64 oracle uses the same grid — it isolates implementation (fp32/2D vs fp64/1D), not resolution.** Result: agreement to 1e-6 (0.038624 vs 0.038623). (3) Fringe frequency measured 5% below the far-field d·p/(ħL) — real Fresnel physics at L = 1.4× the Fraunhofer distance; gate set at 8%, declared. (4) Doubleslit dot curtain: z-spread 4 su + face-on camera preset (art; golden re-frozen pre-commit → `47a67d66`). (5) **All 18 goldens reproduced bit-exact on a 100%-contended GPU** (199–209 s vs 75 s idle) — the order-invariant determinism design validated under the harshest scheduling conditions, unprompted.

**D-017 · 2026-07-12 · ACCEPTED — M5 findings: seed-culling bug, the SR-shifted ISCO, fp32 ledger floor, lens scope.**
(1) **Bug caught by the gate**: newborn BH seeds (mass 0 until their first absorption) were garbage-collected by kBHStep's same-tick compaction — the peak-cell probe proved the core hit 182,720 (1.8× M_FORM) while n_bh stayed 0. Fix: popped BHs carry a −1 sentinel; compaction removes negatives only. The probe stays as a declared observable (`peak_cell_mass`). (2) **Measured physics**: the SR-inertia + Paczyński–Wiita marginally-stable radius lies in (3.3, 4.5)·GM/c²-units — an r=3.3 circular orbit plunges; nexus N4 pins the pure-Newtonian-dynamics limit at exactly 3.0. Same family of interaction as D-016; the isco contract declares the measured band instead of asserting a coefficient. (3) Hawking energy ledger closes to 4.3e-8 relative — the 1e-9 gate was fp64-calibrated; BH mass is fp32 (frame-contract float4), per-tick round-trips inject ulp-dust; gate corrected to 1e-6 (D-011 class). (4) **Lens scope**: v1 ships the screen-space point-lens warp (θ_E, shadow disc, Einstein-ring inversion — the hole's own Hawking glow lenses into a visible ring); the full GARGANTUA per-pixel Kerr geodesic mode and the OptiX-vs-compute spike (D-007 → ORRERY `lens` report) are DEFERRED to the render/art milestone — backlogged, not dropped. (5) Formation threshold M_FORM = 10⁵ (r_s = CELL/4) worked as contracted once the bug fell — no threshold retune needed.

**D-016 · 2026-07-11 · ACCEPTED — M4: the 1PN field term withdrawn after measurement; SR layer ships with exact oracles.**
The einstein contract's 7π combined-precession claim measured 6.41π (stable, fp64) — a beautiful claim, measured, retired (RAYFORMER protocol; derivation parked as Q-006). Shipped model: SR inertia everywhere (v = p/√(m²+p²/c²) — cancellation-free, v < c by construction, N7 discipline structural), Newtonian field, factor-2 photon bending, full proper time dτ = dt√(1+(2Φ−v²)/c²) — photons don't age (the clamp at v=c). Measured: keprel vs exact Sommerfeld 0.50% · clocks τ-ratio 6.3e-4 · photon deflection vs 4GM/bc² 0.83%. Also: LRL endpoint sampling aliases intra-orbit oscillation (6.7% artifact) → least-squares slope over dense samples. **All 8 prior goldens superseded** (relativity is always-on; operator directive "go M4" signs the supersession): kepler 2f93cdfb · threebody c2c572af · cloud 8a0bc6e4 · galaxy dcc87925 · merger bfcfb003 · echo 40a84691 · ratchet 8f4b811e · detector 83ea180d; new: keprel f985e473 · clocks 330c86a7 · photons c4c565de. *Consequence:* every massive particle in every scenario now moves relativistically; the echo still reverses exactly.

**D-015 · 2026-07-11 · ACCEPTED — M3 findings.**
(1) **Merger monotonicity gate corrected pre-golden**: measured mono_frac 0.77 — the dips are real transient compressions of the coarse distribution during collisionless infall (> 0.01 nat, not jitter), so the H-theorem-style gate is ΔS ≥ −0.01 nat per sample at ≥ 0.75, jointly with the rise gate and the echo. (2) **The Loschmidt echo is essentially exact**: S = 14.084043 → 14.570027 → 14.084043 over 2×6000 ticks — fp roundoff stays below the Lyapunov amplification horizon at this window; the arrow-is-statistics claim is now a frozen golden. (3) In-sim Ratchet matches the closed form at 1.5e-5 / 2.3e-3 / 3.3e-3 rel (R=1/4/16, 10⁶ records each) — the M6 collapse mechanic's engine is verified against nexus N6 in production code. (4) SOLV_NONE added; regime bits 0x40/0x80 activated as latches (kDriftK preserves high bits).

**D-014 · 2026-07-11 · ACCEPTED — M2 implementation findings, contract-amended pre-golden.**
(1) **Tiny solver is fp64 internal**: fp32 shared-memory leapfrog measured 3.1e-3 energy drift over 10⁶ ticks on the kepler orbit (pure roundoff — nexus proved the same orbit at 7.4e-10 in fp64). Tiny scenarios are the sim's precision references, and fp64 at N≤32 is free; declared state still round-trips the fp32 frame-contract buffers at batch edges only. Result: drift < 1e-6. (2) **Cloud gets sub-virial dispersion** (σ = 0.35·√(GM/R)): a truly cold pressureless collapse is singular — infinite density in finite time, unresolvable at any fixed dt (measured: 26% heating at the bounce). With physical support: 0.46% drift. (3) **cuFFT dll dependency**: PM links cufft.lib → needs cufft64 runtime dll (toolkit redist; static cuFFT is Linux-only) — dist packaging must ship it (noted for the packaging milestone). (4) PM momentum conservation measured at 2e-6 (fixed-point deposit + spectral solve + FD gradient); galaxy energy drift 1.9% at 10⁴ ticks sits at 95% of its 2e-2 gate — deterministic so golden-stable, but flagged for the M3 art/physics pass.

**D-013 · 2026-07-11 · ACCEPTED — HUD v1 is a CUDA bitmap-font kernel, not ImGui.**
The typed-meters CINEMATIC box needs on-screen text; ImGui wants GLFW + a real GL/Vulkan pipeline + CMake — none of which the single-file P0 app has or needs yet. A 5×7 bitmap font drawn by a CUDA kernel (44 glyphs, 2× scale, drop shadow, physics-cyan/render-amber typing) satisfies the box in ~120 lines with zero dependencies. ImGui arrives with the P1 Vulkan presentation swap (it rides the same pipeline work). *Consequence:* HUD strings are uppercase-limited (glyph set); fine for meters.

**D-011 · 2026-07-11 · ACCEPTED — nexus contract errata: two tolerances corrected by fp64 analysis at implementation.**
(1) N2 |ΔL/L| 1e-12 → 1e-11: leapfrog conserves L exactly in exact arithmetic, but 6.2×10⁷ steps of fp64 roundoff random-walk to ~10⁻¹² (measured 5.8e-13) — the original gate tested the FPU, not the integrator. (2) N7: the 1e-12 cosh↔naive agreement claim is unattainable at γ=10³ (the naive path's own cancellation is ~10⁻¹⁰ there) — domain corrected to γ ≤ 10²; and the "> 1e-3 at γ=10⁷" cancellation demonstration was rounding-luck-dependent (first run measured 4.0e-4 — lucky libm) — replaced by a deterministic form: ladder extended to γ=10⁸ where β rounds to exactly 1.0 and the naive path diverges, plus a 10⁶× error-ratio gate. *Why logged:* both errata are exactly the class of confident-wrong the doctrine exists to catch — the spec claimed precision fp64 cannot deliver; the fix is in the contract changelog, pre-golden, MINOR-scope. *Consequence:* contract v1.0.0 frozen with them applied; golden `ad64f810`.
